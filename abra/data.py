import numpy as np

def read(filename):
    # TODO parse info
    return Data(...)

class Data:

    def __init__(self):
        # TODO implement initialization
        #   pupil_size (1 * n_t)
        #   movement (2 * n_t)
        #   timestamps (1 * n_t)
        #   sample_rate (int)
        #   messages (dict)
        #   events (dict)
        #   calibration (dict)
        return
    
    def trial_start():
        return

    def trial_end():
        return

